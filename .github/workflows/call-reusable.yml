name: Call Reusable Permission Inheritance Test

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  call-test:
    uses: jaserjasar/Test/.github/workflows/reusable-permission-test.yml@main
