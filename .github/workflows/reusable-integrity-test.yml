name: Reusable Integrity Test

on:
  workflow_call:

permissions:
  contents: read

jobs:
  marker:
    runs-on: ubuntu-latest

    steps:
      - name: Show trusted workflow marker
        run: |
          echo "TRUSTED_WORKFLOW_MARKER=BASE_VERSION_2"
