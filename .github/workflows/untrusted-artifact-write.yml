name: Untrusted Artifact Lab

on:
  workflow_dispatch:
  pull_request:

permissions:
  contents: read

jobs:
  produce:
    runs-on: ubuntu-latest

    steps:
      - name: Create harmless artifact
        run: |
          mkdir -p artifact

          cat > artifact/test.sh <<'EOF'
          #!/bin/bash
          echo "UNTRUSTED_ARTIFACT_EXECUTION_MARKER" > /tmp/lab-result.txt
          EOF

          chmod +x artifact/test.sh

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: untrusted-lab-artifact
          path: artifact/test.sh
